{
    "variables": {
        "project_id": "tf-k8s-playground",
        "zone": "europe-west6-a",
        "account_file": "/root/shadow/main-credentials.json"
    },

    "builders": [
        {
            "type": "googlecompute",
            "image_name": "k8s-control-{{ timestamp }}",
            "account_file": "{{ user `account_file` }}",
            "project_id": "{{ user `project_id` }}",
            "source_image": "debian-10-buster-v20200413",
            "zone": "{{ user `zone` }}",
            "ssh_username": "packer"
        }
    ],

    "provisioners": [
        {
            "type": "ansible",
            "user": "packer",
            "playbook_file": "./ansible/k8s-control-install.yml"
        }
    ]
}
